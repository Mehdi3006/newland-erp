# Detekt Placeholder

This configuration is reserved for a future approved Kotlin module. Detekt is versioned in the
Gradle catalog but is not applied because Phase P1 contains no Kotlin source project.

The first Kotlin-module ADR must define the convention plugin, baseline policy, and CI target before
activating this file.
