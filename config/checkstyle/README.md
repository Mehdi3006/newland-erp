# Checkstyle Configuration

`checkstyle.xml` is the repository baseline for future Java modules. `suppressions.xml` is
intentionally empty.

Suppressions require a narrow file or rule match, explanation, owner, and removal issue. A module
may add stricter rules but may not disable this baseline without an approved exception.
