# Playwright Placeholder

This directory intentionally contains no executable browser test. Playwright is configured so a
future approved web application can adopt a consistent test foundation without selecting or
generating a UI during P1.

The first browser application ADR must define environments, base URL, authentication fixtures,
accessibility expectations, and CI execution before adding tests.
