# Libraries

This directory is reserved for future reusable libraries. Phase P1 permits no business or runtime
library source here.

Each future library needs one purpose, one owner, an explicit public entry point, dependency
constraints, tests, and documentation. Libraries must not depend on applications.
